// I am a user who wants to use responsive gamepad, optionally,
// or only half the time. Thus, I want to enable / disable, and check
// if it is enabled

import ResponsiveGamepad from 'responsive-gamepad';

ResponsiveGamepad.enable();

ResponsiveGamepad.disable();

const isEnabled = ReponsiveGamepad.isEnabled();
